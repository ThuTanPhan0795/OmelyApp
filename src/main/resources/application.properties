# Port Configuration
server.port=8080

# Application Name
spring.application.name=E-shop N3XT

# Thymeleaf Configuration
spring.thymeleaf.cache=false

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true

# H2 Database Configuration
spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:file:./data/testdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=thutanphan95@gmail.com
spring.mail.password=springmvc2024
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false

# Hibernate Dialect (for MySQL, uncomment when using MySQL)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Logging Configuration
logging.level.org.springframework.jdbc=debug

# Template Engine Configuration
spring.mvc.view.prefix=classpath:/templates/
spring.mvc.view.suffix=.html
