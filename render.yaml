services:
  - type: web
    name: OmelyApp
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    buildCommand: ""
    startCommand: ""
    env:
      DATABASE_URL: jdbc:mysql://localhost:3306/users_database?allowPublicKeyRetrieval=true&useSSL=false
